# CI writes built project apps into this directory, one per catalogue entry.
# Vite copies public/ into dist/ verbatim, so public/apps/<id>/ is served at
# /apps/<id>/ in both dev and a production build. See APPS.md.
